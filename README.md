# simple-nepal-election-2022-crawler
Fetches the top 3 candidate of Kathmandu valley "Balen" and everyone else

## Requirement

**`python3` and `pip`**

* pip install requests
* pip install bs4
* pip install prettytable


## To run
```python crawler.py```
